h2= "Your search for: " + res.query
#results
  -if (res.query && res.resultsData)
    ul
      - each item in res.resultsData
        li
          a(href= item.url)!= HELPERS.filterBlacklistWords(escape(item.title))
          div!= HELPERS.filterBlacklistWords(escape(item.url))
  -else
    div No results found
